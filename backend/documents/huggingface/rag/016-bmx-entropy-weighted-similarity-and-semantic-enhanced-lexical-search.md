# BMX: Entropy-weighted Similarity and Semantic-enhanced Lexical Search

- Yıl: 2024
- Makale kimliği: RAG_015
- Makale bağlantısı: https://arxiv.org/abs/2408.06643
- Hugging Face veri seti: https://huggingface.co/datasets/GXMZU/llm-rag-agent-papers
- Konu: rag

## İçerik

## BM X : Entropy-weighted Similarity and Semantic-enhanced Lexical Search ∗ ## Abstract BM25, a widely-used lexical search algorithm, remains crucial in information retrieval despite the rise of pre-trained and large language models (PLMs/LLMs). However, it neglects query-document similarity and lacks semantic understanding, limiting its performance. We revisit BM25 and introduce BM X , a novel extension of BM25 incorporating entropyweighted similarity and semantic enhancement techniques. Extensive experiments demonstrate that BM X consistently outperforms traditional BM25 and surpasses PLM/LLM-based dense retrieval in long-context and real-world retrieval benchmarks. This study bridges the gap between classical lexical search and modern semantic approaches, offering a promising direction for future information retrieval research. The reference implementation of BM X can be found in Baguetter, which was created in the context of this work. The code can be found here: https://github.com/mixedbread-ai/baguetter. ## 1 Introduction Information retrieval (IR) remains a fundamental need in human daily life (Zhu et al., 2023) and has found a renewed significance in the era of large language models (LLMs), particularly in retrieval-augmented generation (RAG) (Gao et al., 2023). Among the various IR techniques, BM25 (Robertson et al., 1995) stands out as one of the most influential lexical search algorithms. It is used at the core of many popular search systems such as Lucene, Elasticsearch, or Tantivy. Despite the growing focus on text embedding-based semantic search (Reimers &amp; Gurevych, 2019; Gao et al., 2021; Li &amp; Li, 2023; 2024), BM25 maintains its widespread use in IR due to its high efficiency and strong generalization ability. The BM25 score function is defined as: where D Q , , IDF ( ) · , and F ( ) · denote the document, the query, inverse document frequency (IDF), and term frequency (TF), respectively. qi is the i -th query token of Q . The algorithm operates by first retrieving documents using each query token via an inverted index, then ranking the retrieved documents using the score function. BM25 has two significant limitations: (1) It does not consider the similarity between the query and the document, which could enable a more precise assessment of relevance. (2) As a lexical search algorithm, it lacks semantic understanding, failing to capture linguistic nuances such as synonyms and homonyms. This limitation is a key factor in the underperformance of lexical search compared to domain-specific text embedding semantic search. To tackle these challenges, we propose BM X , a lexical search algorithm that incorporates both similarity and semantics. It comes with three key innovations: (1) Entropy-weighted similarity: We utilize the entropy of each query token to weight the similarity scores between the related documents for that token and the query itself. This mitigates bias from highfrequency tokens, ensuring that less frequent but more informative tokens carry higher similarity weight. (2) Weighted query augmentation (WQA): To introduce semantics into lexical search, we propose a technique that simultaneously processes the original query and corresponding augmented queries within BM X . This also eliminates the need for multiple retrievals and reranking steps, thus resulting in better efficiency. (3) BM25/BM X score normalization: To improve the algorithm's applicability in certain scenarios and benefit threshold setting, we normalize their output scores using an estimated maximum value. We evaluate BM X against BM25 and several popular PLM and LLM-based text embedding models using widely recognized IR benchmarks, including BEIR (Thakur et al., 2021), LoCo (Jon Saad-Falcon, 2024), and BRIGHT (Su et al., 2024). Our experimental results demonstrate the effectiveness and robustness of BM X , consistently outperforming BM25 across all benchmarks. Notably, BM X surpasses 7 billion parameter LLM text embedding model on the long-context retrieval benchmark LoCo (Jon Saad-Falcon, 2024) and outperforms popular proprietary embedding models on the BRIGHT benchmark (Su et al., 2024). In summary, our contributions are as follows: - · We revisit the BM25 algorithm and propose BM X , a lexical search algorithm that addresses the key limitations of its predecessor. - · We introduce an entropy-weighted similarity measure into lexical search to retrieve more relevant documents for a given query and design an efficient weighted query augmentation technique to incorporate semantics into lexical search for improved information retrieval. Additionally, we propose a way of normalizing BM25 scores. - · We demonstrate through extensive experimental results that BM X is effective and consistently outperforms BM25 across various information retrieval benchmarks, even rivaling more complex embedding-based approaches in certain scenarios. - · For convenient use of BM X , we open-source a flexible, efficient, and hackable search engine library Baguetter . ## 2 Related Work This work aligns with the paradigm of lexical search. Lexical search remains one of the most widely adopted information retrieval (IR) approaches, known for its efficiency, interpretability, and generalization capability. Lexical Search In lexical search, TF-IDF (Term Frequency-Inverse Document Frequency) serves as a fundamental tool for scoring and ranking document relevance given a query (Wikipedia). BM25 (Robertson et al., 1995) enhances TF-IDF with carefully designed TF and IDF terms. BM25+ (Lv &amp; Zhai, 2011) refines BM25 by introducing a sufficiently large lower bound for TF normalization. Existing lexical search algorithms typically use query tokens independently to retrieve relevant documents and subsequently rank them based on a score function. These approaches often overlook the overall semantic similarity between the query and the documents, which is crucial in assessing the degree of matching between the query and each individual document. To address this limitation, BM X incorporates similarity techniques to boost the relevance of retrieved documents for a given query, thus improving the search quality. Query Augmentation Query augmentation is an important IR technique for improving the performance of IR systems (Carpineto &amp; Romano, 2012). Traditional approaches primarily rely on lexical ontologies, such as WordNet (Fellbaum, 2010) and FrameNet (Baker et al., 1998; Li et al., 2024), for query augmentation. Yet, these manually annotated lexical resources have limitations in terms of data coverage and currency, which affects their effectiveness in practical applications. Recently, with the surge of LLMs, there has been growing interest in leveraging these models for query augmentation to improve dense retrieval (Baek et al., 2024). Aligning with this trend, we propose the use of LLMs for query augmentation in lexical search. Our approach introduces a one-time query augmentation technique for lexical search, where the retrieval and reranking stages are performed only once, resulting in an augmented retrieval outcome without the need for multiple retrieval cycles. This method aims to enhance efficiency while maintaining the benefits of augmented queries. ## 3 Methodology In this section, we present a detailed exposition of the proposed BM X algorithm. We begin in Section 3.1 by describing the core algorithm, with particular emphasis on the entropy-weighted similarity mechanism. Afterward, Section 3.2 introduces weighted query augmentation (WQA), which substantially enhances the semantic capabilities of BM X . We then describe the normalization process applied to BM X scores in Section 3.3. To demonstrate the broader applicability of our approaches, we extend score normalization and WQA to the widely-used BM25 algorithm in the appendix section B. ## 3.1 Core algorithm of BM X Given a set of documents D consisting of { D 1 , D 2, . . . , Dn } documents where n indicates the total number of documents, and a query Q consisting of { q 1 , q 2, . . . , qm } tokens where m is the total number of query tokens, the BM X score of a document D is calculated as follows: | D | is the length of the document D in tokens, and avgdl is the average document length in the text collection from which documents are drawn. α and β are two hyperparameters. For English IR tasks 1 , we derive the optimal default parameters in the following way: F ( qi , D ) is the number of times that the token qi occurs in the document D . IDF ( qi ) represents the IDF (inverse document frequency) weight of the query token qi . Its calculation is the same as BM25 (Robertson et al., 1995), as follows: where l is the number of documents containing the query token qi , and the corresponding document subset containing qi is D ⊆ D s consisting of { D s 1 , D s 2 , . . . , D s l } . E ( qi ) is the normalized entropy of the query token qi and is expressed as: F ( qi , D s j ) is the number of times that the token qi occurs in the document D s j . E is the average entropy for query Q , as follows: S ( Q D , ) is the similarity between the query Q and document D . There are various similarity measurements that can be used. Here we adopt a simple one, as follows: where | Q ∩ D | denotes the number of common tokens between query and document. In BM X , we address the impact of high-frequency tokens, particularly in documents with numerous repetitions, to maintain balanced probability distributions. We employ the sigmoid function to derive probability values from token frequencies, effectively mapping frequencies exceeding five to similar values. This approach imposes an implicit cap on the maximum effective token frequency, increasing the algorithm's robustness against distortions caused by highly repetitive text. By doing so, BM X ensures relevant contribution from both, maintaining effective retrieval performance across different document structures and content types. By taking the entropy-weighted similarity into account, BM X can retrieve more relevant documents compared to BM25. ## 3.2 WQA: Weighted Query Augmentation Lexical search, while efficient, lacks the semantic understanding of text embedding-based semantic search techniques (Li &amp; Li, 2023). It retrieves documents based solely on query tokens, overlooking, e.g., synonyms and homonyms. To address this limitation, we propose query augmentation using Large Language Models (LLMs): where t is the number of augmented queries, and Q A represents the set of augmented queries { Q A 1 , Q A 2 , . . . , Q A t } . Rather than performing multiple retrieval iterations with the original and each augmented query, we introduce a weighted query augmentation method. This approach combines the original and augmented queries into a single weighted query, eliminating the need for multiple queries: where wi , typically within [ 0, 1 , ] stands for the weight assigned to the i -th augmented query and t for the total number of augmented queries. This weighting scheme introduces semantic understanding into lexical search while preserving computational efficiency. ## 3.3 Normalized BM X The BM X score from equation (2) is unnormalized, which may impact its application in certain scenarios, such as threshold-based retrieval. To address this, we propose a normalized version of BM X using maximum normalization. We begin by estimating the maximum possible score as follows: To calculate the normalized BM X score, we then apply: This normalization keeps BM X scores within interval [ 0, 1 , benefiting threshold retrieval. ] ## 4 Experiment In the following experiment results, BM X refers to the core algorithm, while BM X + WQA indicates BM X with weighted query augmentation. ## 4.1 Experiment Setup Dataset. To comprehensively evaluate the proposed BM X , we conduct experiments on various popular IR benchmarks, including BEIR (Thakur et al., 2021), long-text retrieval LoCo (Jon Saad-Falcon, 2024), and the more realistic and challenging BRIGHT (Su et al., 2024) benchmark. For convenient comparison with embedding-based models, we adopt the MTEB (Muennighoff et al., 2022) variant for BEIR. Implementation. We implemented BM X using Baguetter , an open-source Python library developed in the context of this research. Baguetter is a significantly modified fork of retriv (Bassani, 2023) and provides a unified search interface across sparse, dense, and hybrid retrieval methods. It incorporates an efficient BM25 implementation from the BM25S project (L' u, 2024), which we abstracted to simplify comparisons with BM X . The library supports multi-threaded indexing and searching, making it suitable for large-scale experiments. Its flexible architecture allows for customizable text processing pipelines, custom BM25 algorithms, simple retrieval evaluation, embedding quantization experiments and more. Baguetter 's design philosophy has simplicity at its core, enabling rapid prototyping and efficient iteration on algorithm and model development. This approach sped up our experimental process while maintaining necessary flexibility. The complete source code, including our BM X implementation and evaluation scripts, is available at: https://github.com/mixedbread-ai/baguetter . ## 4.2 BEIR Benchmark Results For the BEIR benchmark, we evaluate on the following datasets: ArguAna (AA), SciDOCS (SD), SciFact (SF), NFCorpus (NFC), TRECCOVID (TCV), Touche2020 (TCH), FiQA2018 (FQA), HotpotQA (HQA), MSMARCO-dev (MM), Fever (FVR), DBPedia (DBP), Quora (QRA), CQADupstack (CQA), and Climate-Fever (CF). For baselines, we choose widelyused embedding models: BGE ( BAAI/bge-large-en-v1.5 ) (Xiao et al., 2023), and Mixedbread ( mixedbread-ai/mxbai-embed-large-v1 ) (Lee et al., 2024). For lexical models, we select BM25 with different variants: ATIRE (Trotman et al., 2014), Robertson (Robertson et al., 1995), BM25+ (Lv &amp; Zhai, 2011), BM25L (Lv &amp; Zhai, 2011), and default kernel Lucene. 2 Weinclude two versions of BM25: An efficient implementation from the BM25S project (L', 2024) and u the implementation in Baguetter . To ensure a fair comparison, both BM25 and BM X utilize an identical text preprocessing pipeline in Baguetter . Table 1: Results on the BEIR benchmark are presented, with NDCG@10 as the primary metric. Model abbreviations are as follows: MiniLM refers to sentence-transformers/all-MiniLM-L6-v2 , BGE to BAAI/bge-large-en-v1.5 (Xiao et al., 2023), and MXBAI to mixedbread-ai/mxbai-embedlarge-v1 (Lee et al., 2024). The best values for embedding and lexical models are highlighted in orange. The results from BM25S (L', 2024) specify precision limited to one decimal place. Table 1 presents the main results on the BEIR benchmark. BM X generally outperforms all BM25 variants, achieving the best results in 11 out of 15 cases. This suggests the effectiveness of BM X and underscores the importance of incorporating query-document similarity in IR. Compared to BM25S, our implementation in Baguetter shows better performance, indicating its high-quality IR abilities. We observe that embedding-based models consistently outperform lexical models. This is attributed to the powerful semantic understanding of embedding-based models, highlighting the importance of semantics in IR.Table 2: Results on BEIR benchmark with weighted query augmentation. The augmentation size is 10. NDCG@10 serves as the main metric. all-MiniLM-L6-v2 indicates the embedding model sentence-transformers/all-MiniLM-L6-v2 . The orange color indicates the best value in each model category. The bold numbers represent the best values across all models. The prompt used in weighted query augmentation is specified in Appendix Section A. To evaluate the impact of semantics on lexical search, we compare lexical search performance with and without query augmentation, as shown in Table 2. The results demonstrate that with the proposed weighted query augmentation (WQA), both BM25 and BM X outperform their non-augmented counterparts. In general, BM X consistently outperforms BM25 both with and without query augmentation, further validating its effectiveness. Interestingly, BM X with WQA even surpassing the performance of the embedding-based model sentence-transformers/all-MiniLM-L6-v2 . ## 4.3 LoCo Benchmark Results For the long-context LoCo retrieval benchmark, we evaluate on Tau Scrolls Gov. Report (GovReport), Tau Scrolls QMSUM (QMSUM), QASPER - Title to Article (Qasper-TA), QASPER - Abstract to Article (Qasper-AA), and Tau Scrolls Summ. Screen (SummScreen). Table 3 presents the results, indicating that even proprietary embedding models struggle with long-context retrieval. In contrast, lexical models generally outperform embedding-based models in this context. The proposed BM X outperforms both BM25 and embedding models in most cases, with the exception of M2-BERT-32768, underlining its strong performance in handling long-context retrieval scenarios. Table 4: Results on the BRIGHT benchmark. NDCG@10 serves as the main metric. † indicates results are retrieved from (Su et al., 2024). UAE-Large is WhereIsAI/UAE-LargeV1 (Li &amp; Li, 2023). MXBAI-Large denotes mixedbread-ai/mxbai-embed-large-v1 (Lee et al., 2024). The orange color indicates the best value in each model category. The bold numbers represent the best values across all models. The prompt used in WQA is specified in Appendix Section A. Table 3: Results of LoCo benchmark. NDCG@10 is the reported metric. † indicates results are retrieved from (Jon Saad-Falcon, 2024). UAE-Large is WhereIsAI/UAE-Large-V1 (Li &amp; Li, 2023). The orange color indicates the best value in each model category. The bold numbers represent the best values across all models. ## 4.4 BRIGHT Benchmark Results Figure 1: (a) shows the index time of BM25 and BM X on different retrieval datasets. (b) shows the corresponding search time. To evaluate the BM X model's performance in realistic retrieval scenarios, we employ the BRIGHT benchmark, utilizing datasets from diverse domains: biology (BIO), earth science (ES), economics (ECO), psychology (PSY), robotics (ROB), StackOverflow (SO), sustainable living (SL), leetcode (LC), pony (PONY), aops (AOPS), theoremqa questions (TQ), and theoremqa theorems (TT). Table 4 presents the results, demonstrating that BM X with weighted query augmentation (WQA) outperforms all baselines, including embedding models and other lexical models. This may be attributed to BM X 's ability to generalize effectively across various domains, while embedding models often struggle with out-of-domain data. The results highlight that the WQA mechanism successfully enhances BM X 's semantic understanding, enabling it to handle realistic retrieval scenarios more effectively than embedding models. BM X achieves this without the need for costly training on massive high-quality datasets, as required by embedding models. ## 4.5 Multilingual Results To evaluate BM X 's performance across languages, we conducted experiments on five multilingual retrieval datasets from Hugging Face: MMarcoRetrieval (Chinese) from CMTEB/MMarcoRetrieval, JaQuAD (Japanese) from SkelterLabsInc/JaQuAD, StrategyQA (Korean) from taeminlee/Ko-StrategyQA, GermanDPR (German) from deepset/germandpr, and FQuAD (French) from manu/fquad2 test. Table 5 presents the results, demonstrating that BM X consistently outperforms BM25 across these multilingual datasets. This suggests the effectiveness of BM X in handling diverse multilingual IR tasks. Table 5: Results of multilingual retrieval. The parameters for BM25 are set to k 1 = 1.2 and b = 0.75. The parameters for BM X are set to α = 0.5 and β = 0.1. The bold numbers represent the best values across all models. ## 4.6 Efficiency Study We compare Baguetter's BM X and Baguetter's BM25 computational efficiency using BEIR benchmark datasets (Thakur et al., 2021) in a single-threaded environment. Figure 1 shows index and search times. BM25 demonstrates faster indexing due to its simpler computations. However, BM X sometimes outperforms BM25 in search speed for larger datasets and overall shows a very similar performance. ## 4.7 Case Study Table 6 presents a comparative case study of BM25, BM X , and BM X with WQA. BM25's second retrieved context misses the 'youngest player' query intent, highlighting its lack of semantic understanding. BM X captures both 'youngest' and 'Premier League' intents in its top 3 results, though the second document refers to 'youngest brother' rather than 'youngest player'. BM X with WQA best matches the intended query, demonstrating the importance of combining lexical similarity with semantic understanding in effective search. Table 6: The top 3 retrieved documents for a given query by different lexical search models on the DBPedia dataset from the BEIR benchmark. The query intents in the retrieved documents are highlighted in dark orange. ## 4.8 Ablation Study Our ablation study on the SciFact dataset examines the impact of the hyperparameters α and β on BM X 's performance (Table 7). Results vary from 65.22 to 69.11, indicating the importance of parameter tuning. The result of the default dynamic parameters is 69.42, outperforming all results in Table 7. While effective for English, these dynamic parameters may require adjustment for other languages or domains. ## 5 Conclusion In this paper, we revisited BM25 and introduced a lexical search algorithm, called BM X . Our approach integrates the proposed entropy-weighted similarity metric with TF-IDF, creating an overall more robust lexical search algorithm. By adding the similarity between queries and documents, BM X demonstrates a better ability to retrieve relevant documents. Moreover, we developed a weighted query augmentation technique that introduces semantic understanding into lexical search, further improving its effectiveness. This helps us bridge the gap between pure lexical matching and semantic comprehension, addressing a longstanding challenge in information retrieval. We introduced Baguetter, an evaluation framework for IR with a reference implementation of BM X . Additionally, we present a normalization technique for both BM X and BM25 scores. Our experiments across different information retrieval benchmarks provide evidence of BM X 's ability to improve lexical search performance. Table 7: BM X results of the SciFact dataset from the BEIR benchmark with different hyperparameters α and β . NDCG@10 is the reported metric. ## A Prompt for Query Augmentation For the query augmentation, we use GPT4 with the prompt in Table 8. ## B Enhanced BM25 Here, we describe the weighted query augmentation for BM25 and normalized BM25. Table 8: Prompt used in GPT4 for weighted query augmentation. ## B.1 Weighted Query Augmentation for BM25 The weighted query augmentation for BM25 is similar to that for BM X . First, we augment the query using LLMs as follows: where t is the number of augmented queries, and Q A denotes the set of augmented queries { Q A 1 , Q A 2 , . . . , Q A t } . Then, the weighted query augmentation for BM25 is as follows: where wi is the weight for the i -th augmented query, typically set between 0 and 1. ## B.2 Normalized BM25 Similar to BM X , we first estimate the maximum value of the BM25 score: The normalized BM25 score is then calculated as follows:
